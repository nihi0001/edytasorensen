---

---

<!-- martin eksempel -->
<form>
  <label for="name">Name</label>
  <input type="text" name="name" required />

  <label for="email">email</label>
  <input type="text" name="email" required />

  <label for="text">comments</label>
  <input type="text" name="comments" />

  <button>SEND</button>
</form>

<script>
  const url ="https://nizfrlmeuslmzbvoilai.supabase.co/rest/v1/contact_request";
  const key ="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5pemZybG1ldXNsbXpidm9pbGFpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MzMxNzM2MDAsImV4cCI6MjA0ODc0OTYwMH0.RQ3OCApyz7TpKmzgRC189LxU9mOzQLPHTIxEIIeFO2Y";

  const form = document.querySelector("form");
  form.addEventListener("submit", sendForm);

  function sendForm(event) {
    event.preventDefault();
    const formData = form.elements;
    let name = formData.name.value;
    let email = formData.email.value;
    let comments = formData.comments.value;

    fetch(url, {
      method: "POST",
      headers: {
        apikey: key,
        Accecpt: "application/json",
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name, email, comments }),
    }).then(() => {
      form.reset(); // Reset form efter "send"
    }).catch((error) => {
      console.error("Error:", error); // errors
    });
  }
</script>
